# Docs for the Azure Web Apps Deploy action: https://go.microsoft.com/fwlink/?linkid=2134798
# More GitHub Actions for Azure: https://go.microsoft.com/fwlink/?linkid=2135048

name: demo-github-actions-ci-cd

on:
  push:
    branches:
      - master
      - develop
      - 'release/**'

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: 'Checkout Github Action'
      uses: actions/checkout@master


    - name: Set up .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '3.1.301'

    - name: Build with dotnet
      run: dotnet build ContosoCrafts.Website/ContosoCrafts.Website.csproj --configuration Release

    - name: dotnet publish
      run: dotnet publish ContosoCrafts.Website/ContosoCrafts.Website.csproj -c Release -o ${{env.DOTNET_ROOT}}/myapp
      
    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v3.0.0
      with:        
        name: artifact        
        path: ${{env.DOTNET_ROOT}}/myapp         
        if-no-files-found: warn        
        retention-days: 30
  
  deploy-to-dev:
      needs: build
      name: Deploy to gcp
      runs-on: windows-latest
      environment:
        name: dev
        url: http://dev.my-app.com
      steps:
      - name: Download a Build Artifact
        uses: actions/download-artifact@v3.0.0
        with:          
          name: artifact
          path: ${{env.DOTNET_ROOT}}/myapp
      
      - name: Deploy
        run: |
          ping 35.244.42.144
          telnet 35.244.42.144
          echo "check the results of ping & tracert"
          net use "${{secrets.DEV_HOSTPATH}}" /user:"${{secrets.DEV_USER}}" "${{secrets.DEV_PASSWORD}}"
          robocopy ${{env.DOTNET_ROOT}}/myapp/obj/Release/myapp "${{secrets.DEV_HOSTPATH}}" /s /e


  deploy-to-uat:
    needs: build
    name: Deploy to UAT
    environment:
      name: uat
      url: http://uat.myapp.com
    runs-on: ubuntu-latest
    steps:
    - name: Deploy
      run: echo Deploying to UAT environment
    

